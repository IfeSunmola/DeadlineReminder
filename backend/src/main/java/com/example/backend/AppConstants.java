package com.example.backend;

/**
 * @author Ife Sunmola
 */
public class AppConstants {
	// used when authenticating a user that has been verified. The authentication is done without the user's password
	// which now after typing it out, I realize how stupid it is but, it is what it is
	public static final String NO_PASSWORD_NEEDED = "No password needed to authenticated this user";
}