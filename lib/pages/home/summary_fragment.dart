import 'package:flutter/material.dart';

class SummaryFragment extends StatelessWidget {
  const SummaryFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Summary", style: TextStyle(fontSize: 30)),
    );
  }
}
