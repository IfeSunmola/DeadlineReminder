import 'package:flutter/material.dart';

class DeadlinesFragment extends StatelessWidget {
  const DeadlinesFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Deadlines", style: TextStyle(fontSize: 30)),
    );
  }
}
