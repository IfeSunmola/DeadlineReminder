import 'package:flutter/material.dart';

class RestoreFragment extends StatelessWidget {
  const RestoreFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Restore", style: TextStyle(fontSize: 30)),
    );
  }
}
